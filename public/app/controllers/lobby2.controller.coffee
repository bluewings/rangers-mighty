'use strict'

angular.module 'rangers'
# .controller 'CommonHomeController', ($scope, $element, $window, Auth, $state, $timeout, corsStorage, isLoggedIn, currentUser) ->  
.controller 'Lobby2Controller', ($scope) ->  
  vm = @
  return